# macOS Finder metadata
.DS_Store

# Python bytecode cache
__pycache__/
*.pyc

# Claude Code runtime lock files
.claude/scheduled_tasks.lock

# Windows Zone.Identifier files (WSL download metadata)
*Zone.Identifier

# Environment / secrets
.env
.env.local

# Stray top-level preview lockfile (the tracked one lives in preview/web/)
week0_explore/preview/package-lock.json
